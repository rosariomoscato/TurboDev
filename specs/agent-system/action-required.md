# Action Required: Agent System

## Before Implementation

- [ ] **Install gray-matter** — Run `npm install gray-matter` to add the Markdown frontmatter parser dependency required by the agent parser task.

## During Implementation

No manual steps required during implementation.

## After Implementation

No manual steps required after implementation.

---

> These tasks are also referenced in context within the relevant task files.
