# Action Required: TurboDev CLI

Manual steps that must be completed by a human. These cannot be automated.

## Before Implementation

- [ ] **OpenRouter account** — Create an account at https://openrouter.ai to be able to generate an API key (only needed for testing, not for implementation)

## After Implementation

- [ ] **npm publish** — When ready to distribute, publish the package to the npm registry under the name `turbodev`
- [ ] **OpenRouter API key** — Generate an API key from OpenRouter to test the tool end-to-end

---

> These tasks are also referenced in context within the relevant task files.